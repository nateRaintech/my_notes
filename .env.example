# Optional: SMTP config for the autodev runner's "all items blocked" email alert.
# Copy to .env and fill in to enable alerting. Leave unset to disable (the runner
# just prints a warning and continues). .env is git-ignored.

SMTP_SERVER=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
ALERT_FROM=
ALERT_TO=
